<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= restaurante.nombre %> - Hamburguesas</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-image: url('/imagenes/wallpaper.jpeg');

            color: #343a40;
            font-family: 'Roboto', sans-serif; 
        }

        .container {
            background-color: #ffffff;
            border-radius: 1rem;
            padding: 2rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        h1 {
            color: #000000;
            margin-bottom: 2rem;
            font-size: 2rem;
            text-align: center;
        }

        .card {
            border: 1px solid #ddd;
            border-radius: 0.5rem;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: transform 0.2s ease-in-out;
        }

        .card:hover {
            transform: scale(1.05);
        }

        .card-img-top {
            border-top-left-radius: 0.5rem;
            border-top-right-radius: 0.5rem;
            object-fit: cover;
            height: 200px;
        }

        .card-body {
            padding: 1.5rem;
            text-align: center;
        }

        .card-title {
            font-size: 1.25rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: #333;
        }
        .card-title a {
            color: black;
            text-decoration: none;
        }

        .card-title a:hover {
            color: #007bff;
            text-decoration: underline;
        }

        .btn-primary {
            background-color: #007bff;
            border-color: #007bff;
            border-radius: 0.25rem;
            padding: 0.75rem 1.5rem;
            font-size: 1rem;
            font-weight: 700;
            transition: background-color 0.2s ease-in-out;
        }

        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #004085;
        }

        .alert-info {
            border-color: #b8daff;
            background-color: #d1e7fd;
            color: #0c5460;
            text-align: center;
            font-weight: 500;
        }
    </style>
</head>

<body>
    <div class="container mt-4">
        <h1><%= restaurante.nombre %> - Hamburguesas</h1>

        <div class="row">
            <% if (burgers.length > 0) { %>
                <% burgers.forEach(burger => { %>
                    <div class="col-md-4 mb-4">
                        <div class="card">
                            <img src="/imagenes/burgers/<%= burger.id %>.jpg" alt="Foto de <%= burger.nombre %>"
                                class="card-img-top">
                            <div class="card-body">
                                <h5 class="card-title mb-3">
                                    <a href="/burgers/<%= burger.id %>" > <%= burger.nombre %></a>
                                </h5>
                                <a href="/burgers/<%= burger.id %>" class="btn btn-primary">Ver más</a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-md-12">
                    <div class="alert alert-info" role="alert">
                        No hay hamburguesas disponibles en este restaurante.
                    </div>
                </div>
            <% } %>
        </div>

        <a href="/restaurantes/catalog" class="btn btn-primary mt-4">Volver al catálogo</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js"
        integrity="sha384-oBqDVmMz4fnFO9sg6K3D37P3KgyxZb2LO68tX53OhhlrOnA69J8MDo4J7v3D4u5S"
        crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"
        integrity="sha384-4dQ2p6i8p0CU1hwH5se+Ygk0d7d3h5l5DFkcYdcnFlYRX+RbTxRih9KsoF2HEjKh"
        crossorigin="anonymous"></script>
</body>

</html>
