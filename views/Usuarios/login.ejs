<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Burger Week - Inicio de Sesión</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-image: url('/imagenes/wallpaper.jpeg');
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Roboto', sans-serif;
        }

        .card {
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }

        .card-title {
            text-align: center;
            font-weight: bold;
            font-size: 1.5rem;
            color: #000000;
        }

        .btn-primary {
            background-color: #28a745;
            border-color: #28a745;
        }

        .btn-primary:hover {
            background-color: #218838;
            border-color: #1e7e34;
        }

        .alert-warning {
            background-color: #fff3cd;
            border-color: #ffeeba;
            color: #856404;
        }

        .error-label {
            color: #dc3545;
            font-size: 0.9rem;
        }

        .form-control {
            border-radius: 0.375rem;
        }

        label {
            font-weight: bold;
        }

        a {
            text-decoration: none;
            color: #28a745;
        }

        a:hover {
            color: #218838;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-8 col-sm-10 col-xxl-4">
                <div class="card mt-5">
                    <div class="card-body">
                        <h5 class="card-title">Inicio de Sesión</h5>
                        <form method="post" action="">
                            <% if (errors) { %>
                                <div class="alert alert-warning">
                                    <%= errors.message %>
                                </div>
                            <% } %>
                            <div class="mt-3">
                                <label for="email">Email</label>
                                <input required class="form-control" type="email" name="email" placeholder="Email" id="email">
                                <% if (errors?.email) { %>
                                    <div class="error-label">Por favor ingrese un email válido</div>
                                <% } %>
                            </div>
                            <div class="mt-3">
                                <label for="password">Password</label>
                                <input required class="form-control" type="password" name="password" placeholder="Ingrese la contraseña" id="password">
                                <% if (errors?.password) { %>
                                    <div class="error-label">Por favor ingrese una contraseña</div>
                                <% } %>
                            </div>
                            <div class="mt-4 text-center">
                                <input class="btn btn-primary btn-block" type="submit" value="Enviar">
                            </div>
                            <div class="mt-3 text-center">
                                <label>¿No tienes una cuenta?</label>
                                <a href="/usuarios/create" class="btn btn-link">Crear cuenta</a>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js"></script>
</body>

</html>
